import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.financeiromain',
  appName: 'financeiro-main',
  webDir: 'dist'
};

export default config;
